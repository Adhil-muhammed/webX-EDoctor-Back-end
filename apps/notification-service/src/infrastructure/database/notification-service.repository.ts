import type { NotificationServiceModel } from './notification-service.model';

export class NotificationServiceRepository {
  async findById(id: string): Promise<NotificationServiceModel | null> {
    void id;
    return null;
  }
}
